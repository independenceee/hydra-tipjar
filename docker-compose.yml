version: "3.9"

services:
    app:
        build:
            context: .
            dockerfile: Dockerfile

        container_name: tipjar
        restart: unless-stopped

        ports:
            - "3000:3000"

        env_file:
            - .env

        environment:
            - NODE_ENV=production
            - PORT=3000
